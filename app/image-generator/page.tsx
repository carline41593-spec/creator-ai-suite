"use client";import {useState} from 'react';import {generateImage} from '../../lib/fluxClient';export default function Page(){const[p,sP]=useState('');const[l,sL]=useState(false);const[r,sR]=useState('');async function go(){sL(true);const d=await generateImage(p);sR(d?.image_url);sL(false);}return(<div><h1 className='text-2xl font-bold mb-4'>Image Generator</h1><textarea value={p} onChange={e=>sP(e.target.value)} className='w-full p-4 bg-neutral-800 text-white rounded mb-4'/><button onClick={go} className='px-6 py-3 bg-gold text-black font-bold rounded'>{l?'Generating...':'Generate Image'}</button>{r&&(<div className='mt-6'><img src={r} className='rounded-lg'/></div>)}</div>)}